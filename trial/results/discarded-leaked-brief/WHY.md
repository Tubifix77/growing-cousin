# Discarded: run against a brief that leaked the answer

`20260910-203555_gemma4_e2b.jsonl` is a partial run started after the v3 brief
fixes but BEFORE the answer leak was found and removed. The brief still named
`wake_catchup_fetcher` together with the exact fact that disqualified it, so any
semantic number from this run is worthless.

Kept rather than deleted, because the discarded method is worth more than the
answer it produced.
